import { PublicPage } from '../components/PublicLayout';
const flow = ['Real-world water', 'Sensors', 'ESP32', 'REST API', 'FastAPI backend', 'Data validation', 'Pandas + NumPy', 'Feature engineering', 'AI / ML analysis', 'Results', 'Dashboard', 'Early warning'];
const sensors = [['pH sensor', 'Measures the acidity or alkalinity of water.'], ['Turbidity sensor', 'Measures water cloudiness caused by suspended particles.'], ['Temperature sensor', 'Measures water temperature.'], ['TDS sensor', 'Measures total dissolved solids.'], ['Conductivity sensor', 'Measures electrical conductivity related to dissolved ions.']];
const steps = ['Sensors collect measurements.', 'ESP32 receives and transmits measurements through the REST API.', 'FastAPI receives data and validates values.', 'Pandas and NumPy process measurements; feature engineering prepares them for analysis.', 'Isolation Forest detects abnormal patterns and Random Forest classifies risk.', 'Sensor-health analysis evaluates reliability; water-quality status and early warnings are generated.', 'Results are displayed in the protected dashboard.'];
export default function HowItWorks() { return <PublicPage eyebrow="How it works" title="From real-world water to an early warning"><div className="flex flex-wrap items-center gap-2">{flow.map((item, index) => <><span className="rounded-lg border border-cyan-400/20 bg-cyan-400/5 px-3 py-2 text-xs font-medium text-cyan-200">{item}</span>{index < flow.length - 1 && <span className="text-cyan-500">→</span>}</>)}</div><div className="mt-12 grid gap-4 lg:grid-cols-2"><div className="glass-card p-6"><h2 className="font-semibold text-white">How the sensors work</h2><div className="mt-4 space-y-3">{sensors.map(([title, body]) => <div key={title}><h3 className="text-sm font-medium text-cyan-300">{title}</h3><p className="text-sm text-slate-400">{body}</p></div>)}</div></div><div className="glass-card p-6"><h2 className="font-semibold text-white">How data is analyzed</h2><ol className="mt-4 space-y-3">{steps.map((step, index) => <li className="flex gap-3 text-sm leading-6 text-slate-400" key={step}><span className="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-cyan-400/10 text-xs text-cyan-300">{index + 1}</span>{step}</li>)}</ol></div></div></PublicPage>; }
